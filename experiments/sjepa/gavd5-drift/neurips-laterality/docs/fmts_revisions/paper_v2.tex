\documentclass{article}
\PassOptionsToPackage{numbers,sort&compress}{natbib}
\usepackage[dblblindworkshop]{neurips_2026}
\workshoptitle{Foundation Models for Temporal Systems: From Forecasting to World Modeling}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,graphicx,booktabs,longtable,array,calc}
\usepackage{microtype,url}
\usepackage[hidelinks,unicode]{hyperref}
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\providecommand{\pandocbounded}[1]{#1}
\setlength{\emergencystretch}{1em}
\title{Hidden-Feature Matching and Left-Right Movement in Gait Representations}
\author{Anonymous Author(s)}
\hypersetup{pdftitle={Hidden-Feature Matching and Left-Right Movement in Gait Representations},pdfauthor={},pdfcreator={LaTeX}}
\begin{document}
\maketitle
\begin{abstract}
A representation of an evolving system should support estimates of
observable movement as well as its training task. We examine this
requirement in gait using a small skeleton Joint-Embedding Predictive
Architecture (JEPA). The model predicts hidden skeleton features; a
separate regression estimates a signed contrast between left and right
movement speeds from its frozen encoder. In source-video
cross-validation on 625 clips from 93 videos, trained predictors more
consistently match hidden features from the correct clip than from
another video. Yet with an expanded feature summary, mean
source-balanced \(R^2\) for the movement estimate falls from 0.22 at
initialization to 0.10--0.11 after training. Motion-weighted and
connected-region masks do not establish an improvement over their own
random references. This focused negative finding connects latent
prediction to evaluation of observable movement: success on
hidden-feature matching did not improve the specified movement estimate
under the tested training and readout procedure. Future-only prediction
remains a separate test.
\end{abstract}
\section{Introduction}\label{introduction}

Walking is an evolving system: coupled body landmarks change position
over time, and movement depends on their trajectories. Gait rhythm and
coordination motivate temporal modeling, although video-derived
coordinates describe only part of the physical process. Temporal world
modeling aims to represent such a system well enough to predict its
observable behavior. A practical evaluation question is whether a
representation trained through prediction preserves information needed
for a specified movement measurement.

We study that question with S-JEPA \citep{sjepa}, using a left--right
contrast of median coordinate speeds. Clinical gait asymmetry motivates
the setting \citep{stroke}; our contrast has no clinical validation. Our
central null is that JEPA training provides no improvement over the same
encoder at initialization when a frozen readout estimates this contrast
on held-out source videos. The current expanded-readout and mask
comparisons are exploratory analyses on a repeatedly examined cohort;
they do not retrospectively become registered tests.

The contribution is a domain-specific evaluation of predictive
representations, relevant to the reliability of temporal foundation
models. The present study establishes neither foundation-model scale nor
a gait simulator.

\section{Study design}\label{study-design}

\textbf{Data and observable.} Our selected GAVD subset \citep{gavd}
contains 666 annotations, 642 matching pose archives and, after quality
checks, 625 clips from 93 source videos. Each archive has shape
\(T_i\times33\times4\): three estimated coordinates plus landmark
visibility, with original frame numbers and frame rate. Coordinate units
are image normalized with inferred depth; they are not calibrated
physical speeds.

The movement path uses observed coordinates, pelvis centering and a
body-width scale, without gap filling or resizing. For five
pairs---shoulders, knees, ankles, heels and foot tips---we calculate
median speeds \(m_{L,k},m_{R,k}\) using original timestamp differences.
Both sides must be observed at both ends of at least eight common
transitions for every pair. The signed target is

\[y=\frac{1}{5}\sum_{k=1}^{5}\frac{m_{L,k}-m_{R,k}}{m_{L,k}+m_{R,k}+10^{-8}}.\]

Anatomical reflection exchanges left/right landmarks and validity flags
and flips the centered horizontal coordinate, giving \(y(Mx)=-y(x)\).
Hips define the pelvis and are excluded from the contrast because
centering equalizes their speed magnitudes. The statistic does not
measure phase, stride timing or loading; reversing a trajectory together
with its sampling intervals preserves its speed magnitudes.

\textbf{Encoder input.} A landmark is observed when visibility is at
least 0.45 and all coordinates are finite. A separate encoder path fills
interior gaps of at most four samples, treating filled positions as
valid. After pelvis centering and body-width normalization, coordinates
and validity are linearly resized to 64 positions by sequence index.
Original time intervals are discarded. Resized validity must reach
0.999; invalid coordinates are zero. Each joint's four-position patch
projects 12 coordinates to 96 features, giving a \(16\times33\) grid. A
patch is valid only if all four positions are valid. Anatomical
identities and prepared positions survive this operation; the original
trajectory, duration and cadence need not.

\begin{figure}[htbp]
\centering
\includegraphics[width=\linewidth]{assets/v2/figures/training_pipeline_compact.pdf}
\caption{Two measurement paths and the completed JEPA training grid. Poses and mask cells are schematic. The movement target reaches only the separate regression evaluation. The online encoder has visible context on either side of a hidden position; the teacher and full-view regularizer access complete prepared clips.}
\end{figure}

\textbf{Training.} The grid uses 1,200 AdamW updates per encoder
(architecture and settings in Appendix A). The online encoder zeroes
hidden patch content before adding joint and position embeddings; the
predictor substitutes learned mask tokens at target positions.
Cross-entropy matches centered teacher feature distributions at hidden
valid positions, averaged within clips and then across clips. A teacher
follows the online weights by exponential moving average without
gradients. A full-view VICReg term \citep{vicreg}, weighted 0.05, pools
twelve gait joints across two geometric views. Thus the total objective
also accesses complete clips. Training uses CUDA BF16 and evaluation
FP32.

We compare three motion conditions and two region conditions across five
folds and five seeds: 125 encoders. The motion conditions use random
sampling, a MAMP-style sampler \citep{mamp}, and a robust motion
mixture. Their target count is half the batch's smallest valid
twelve-joint gait pool, rounded down, sampled across all 33 joints.
Connected regions hide six connected landmarks over eight of sixteen
blocks, with a separate count-matched random reference. Each comparison
matches initial weights, source draws, views and update counts. No
reflection augmentation or reflection loss appears in this grid.

\textbf{Held-out readout.} Five outer folds keep each video's clips and
derived views together. Fresh encoders fit only outer-training videos
with source-balanced sampling. All overlapping windows, mirrors and
augmentations inherit the source split. The encoder is frozen while
ridge regression, a linear model with a coefficient penalty, estimates
\(y\). Three inner source folds select the penalty using only
outer-training labels; imputation and scaling are fitted within each
inner training set and then refitted with the regression on all
outer-training sources. Inner-validation clips may have contributed
without labels to outer-training encoder fitting.

Both encoders use bilateral mean features (960 inputs), expanded with
standard deviations, absolute adjacent-block feature changes and ten
validity-support fractions (2,890 inputs). We pool outer-test
predictions within each seed, weighting each video equally, then average
the five \(R^2\) values. Paired 95\% source-bootstrap intervals resample
videos with their clips and seed predictions together (2,000 resamples).
They condition on the fitted models and existing splits. Repeated seeds
and mask draws do not add independent sources.

\section{Findings}\label{findings}

\textbf{Movement readout falls after training.} The expanded initial
representation reaches mean \(R^2=0.223\), compared with
\(0.101\)--\(0.114\) for trained teachers (Figure 2). All five
conditions decrease in every seed, with greater mean absolute error.
Paired trained-minus-initial intervals are below zero in every
condition. Final online encoders also score below their matched initial
encoders.

\begin{center}
\begin{tabular}{lrrl}
\toprule
Training mask & Mean $R^2$ & $\Delta R^2$ & 95\% source interval \\
\midrule
Random (motion) & 0.114 & -0.109 & [-0.170, -0.041] \\
MAMP-style & 0.113 & -0.110 & [-0.165, -0.051] \\
Motion mixture & 0.114 & -0.108 & [-0.171, -0.042] \\
Random (region) & 0.109 & -0.113 & [-0.166, -0.060] \\
Connected region & 0.101 & -0.122 & [-0.183, -0.055] \\
\bottomrule
\end{tabular}
\end{center}

Table 1: trained teacher versus matched initialization; exploratory
paired intervals conditional on fitted models. Differences precede
rounding.

The compact initial readout scores 0.071; expanding it increases \(R^2\)
by 0.152. The added components mix variation, feature changes and
observation support, so the gain cannot be assigned specifically to
temporal order. The initial control retains the pretrained pose
detector, input preparation, anatomical identities and supervised
regression. A 264-value direct-pose summary reaches \(R^2=0.035\); this
limited baseline does not bound stronger coordinate readouts.

\begin{figure}[htbp]
\centering
\includegraphics[width=\linewidth]{assets/v2/figures/learning_results.pdf}
\caption{Different outcomes from the same fitted models. A: frozen movement readout; points are five seed scores, and short vertical marks their mean. B: correct-clip feature error is lower in 375 of 375 trained diagnostic checks, versus 33 of 75 distinct initial checks. These checks reuse models and videos.}
\end{figure}

\textbf{Correct-clip features become easier to match.} Each diagnostic
compares source-weighted mean squared feature error against the correct
clip and valid targets from another source, holding the evaluated clips
and hidden positions fixed. All 375 trained checks favor correct
targets; 33 of 75 initial checks do so. The trained checks cover 125
fitted models under three evaluation masks (left-leg gap, right-leg gap
and scattered gap). Initial checks cover 25 matched fold/seed controls
under the same three masks, with copies shared between experiment
families counted once. This preference could involve pose, viewpoint or
missingness as well as motion. Teachers and target distributions change
during training, so raw training losses cannot rank representation
quality across conditions.

\textbf{Changing mask geometry does not establish better readout.} The
inspection draws confirm that motion samplers select higher-motion
targets and that connected regions remove immediate temporal brackets.
Motion masks hide approximately 17\% of valid all-landmark tokens;
regions hide 9.9\%. Their readout comparisons therefore use distinct
random references. MAMP-style, motion-mixture and region differences are
respectively \(-0.0008\), \(+0.0005\) and \(-0.009\), with intervals
\([-0.020,0.015]\), \([-0.015,0.015]\) and \([-0.033,0.018]\). Intervals
spanning zero leave benefits uncertain, rather than establishing
equivalence.

\textbf{Preparing input changes the measurement.} A saved comparison
recalculates the target after input preparation: 623 clips from 92
sources have finite values on both paths, with source-weighted sign
agreement 70.4\% and calculation-agreement \(R^2=0.218\). This is
agreement between two calculations of the observable, not a learned
model score. Neither the achievable readout accuracy nor an individual
preprocessing cause follows from it.

\section{Interpretation and next
experiment}\label{interpretation-and-next-experiment}

The two evaluations answer different questions. JEPA training improves
preference for the correct clip's hidden features, while a fixed
regression procedure estimates its left--right speed contrast less
accurately. Poorer readout performance does not imply complete
destruction of movement information. In particular, 49/125 teacher fits
and 96/125 online fits select the largest ridge penalty, 10,000. Wider
training-only selection, support-only controls and summaries of matched
dimension would help separate feature geometry from readout limitations.
Person identities, reuploads, pose errors and recording cues remain
possible explanations beyond what source holdout controls.

A next experiment would decode future coordinates from past-only inputs
at common valid endpoints and physical-time horizons. Fit and select
decoders on training sources only; compare last-position,
constant-velocity, direct-pose, initial-encoder and
mismatched-training-future controls. Future coordinates and visibility
must not change prefix preparation, normalization, masks or predictions.
The retained past-feature readout and future-feature decoding studies
have synthetic training evidence; the real-data horizon census
establishes eligibility only. Decoding observed future-teacher features
diagnoses the decoder; decoding features predicted from the past
evaluates forecasts. Multiple horizons from one prefix are not recursive
rollouts.

The finding is conditional on this cohort, recipe and readout.
Forecasting and simulation require tests of future behavior beyond the
whole-clip contrast.

\section{Related work}\label{related-work}

Skeleton action recognition motivates learned feature targets
\citep{sjepa}, motion targets \citep{mamp} and connected-body masking
\citep{slim}. We vary masks within one JEPA objective without
reproducing these complete methods. Video anticipation and planning
\citep{vjepa2}, together with transformation-sensitive representation
learning \citep{seqjepa}, show why evaluation depends on the intended
behavior. Here the endpoint is an observed gait quantity: comparing its
recovery before and after training complements those task evaluations.

\clearpage
\bibliographystyle{unsrtnat}
\bibliography{assets/v2/references}
\clearpage
\appendix
\section{Measurement and implementation
details}\label{measurement-and-implementation-details}

The completed recipe uses 96 features, encoder/predictor depths 4/2,
four attention heads, batch size 20, 1,200 AdamW updates, learning rate
0.001, weight decay 0.05 and teacher momentum 0.999.

The archive coordinates are \(x=(x_0+uW_c)/W\), \(y=(y_0+vH_c)/H\), and
\(z=dW_c/W\), where crop origin and size are \((x_0,y_0),W_c,H_c\),
frame size is \(W,H\), and \(u,v,d\) are pose estimates. Unequal
image-axis scaling and inferred depth remain after body normalization.
The scale is the median of valid shoulder and hip widths. The movement
path requires observed hips; the encoder uses the median valid pelvis
midpoint for missing hips (zero if none is available). No boundary
extrapolation fills gaps.

The teacher receives the unaugmented prepared clip; the masked online
path receives a geometric view. Two full online views use rotations up
to 8 degrees in the horizontal-depth plane and translations up to 0.03
in image-plane coordinates. Target/predictor temperatures are 0.06/0.10;
the teacher center uses momentum 0.9. Gradient norm is clipped at 1. The
twelve-joint regularizer pool is shoulders, hips, knees, ankles, heels
and foot tips. Its projector is a separate training component. It
combines invariance, variance and covariance penalties with internal
weights 25, 25 and 1, then overall weight 0.05.

The expanded summary uses common left/right validity within each pair;
increments require consecutive common valid blocks. Each pair
contributes differences and sums of means, standard deviations and mean
absolute feature increments (six 96-vectors), plus two support
fractions. Thus \(5\times6\times96+10=2,890\). Missing support gives
zero summary values. Temporal positions in this summary are prepared
block indices, not original timestamps. Ridge penalties are selected
from \(0.01,0.1,1,10,100,1000,10000\).

Source-balanced \(R^2\) uses weights \(w_{vi}=1/(Vn_v)\) and weighted
test mean \(\bar y_w\):

\[R_w^2=1-\frac{\sum_{vi}w_{vi}(y_{vi}-\hat y_{vi})^2}{\sum_{vi}w_{vi}(y_{vi}-\bar y_w)^2}.\]

The test mean defines this metric's denominator, not a deployable fitted
baseline. Outer test folds contain 19, 19, 19, 18 and 18 videos and 189,
182, 72, 77 and 105 clips. Source IDs and split membership are checked;
no persistent participant identities or complete cross-upload content
comparison establish independence across different IDs. Annotation
retrieval/version records are incomplete.

\section{Supporting results and
reproducibility}\label{supporting-results-and-reproducibility}

These exploratory percentile intervals use bootstrap seed 812, retain
all five fitted seeds and have no familywise or development-selection
adjustment. The numerical supplement preserves aggregate seed scores and
recorded intervals. The executed report identifies 125,000 prediction
rows and 200 pooled score rows; a saved audit records their
recomputation. Raw predictions and checkpoints are unavailable in the
revision environment, so this revision checks aggregate consistency
without claiming to repeat that bootstrap or inference.

A separate reflection-augmentation comparison has real-data summary
evidence: augmentation reduces the specified teacher-token reflection
discrepancy by \(\Delta q=-0.008\) \([-0.010,-0.007]\), while its
readout difference is \(\Delta R^2=0.004\) \([-0.006,0.013]\). This uses
its own recipe, four inner readout folds and compact summary; it is not
pooled with the motion grid. An explicit reflection loss has synthetic
evidence only. Likewise, a readout forced to take the form
\(g(x)=[h(x)-h(Mx)]/2\) reverses sign algebraically without establishing
learned movement information.

GAVD annotations describe the cohort, not affected side or treatment
response. Uncalibrated coordinates and absent clinical reference
measurements preclude a clinical interpretation of \(y\). Public
annotation access does not authorize redistribution of separately hosted
videos or derived poses; neither is included in the supplement.

\textbf{AI assistance.} An AI assistant supported author-side evidence
reconciliation, literature checking, manuscript revision, figure
programming and adversarial checks. No new model training was performed
for this revision. Responsibility for the submitted text, figures,
claims and references remains with the authors.

\end{document}
