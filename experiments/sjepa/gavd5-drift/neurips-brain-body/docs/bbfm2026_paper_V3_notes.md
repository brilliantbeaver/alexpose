# V3 revision notes

An earlier title revision selected **What Does Pretraining Add Beyond Anatomy? Lessons for Body Foundation Models from Gait Laterality**. It foregrounds the controlled comparison of trained and untrained features with anatomical inputs and prediction methods held fixed. "Lessons for" connects the compact-encoder experiment to body foundation-model research without implying that large foundation models were evaluated. The abstract and methods retain the JEPA-style implementation and video-derived pose context.

That title was synchronized across Markdown, LaTeX, and PDF metadata. V3-only display metadata specified three title lines, breaking after the question and after "Models" so that "from Gait Laterality" remained together. The display lines reproduced the plain title exactly when joined, and the template retained the official title font and size. Visual review confirmed a complete abstract on page 1 and a five-page main paper, with ten pages overall. The build had no missing citations, missing glyphs, or overfull/underfull boxes. The abstract, body, figure assets, and V2 files were unchanged by that revision.

The preceding title-only update used **Gait Laterality as a Test of JEPA-Style Pretraining on Video-Derived Pose**, synchronized across Markdown, LaTeX, and the PDF, including PDF metadata. That title wrapped onto two lines, and the complete abstract remained on the first page. The paper retained five main-text pages and ten pages overall; the longer title moved two concluding Results lines to the top of page 5. Visual review found no clipping or stranded headings, and the mild underfull-page warning required no adjustment. The abstract, manuscript body, figures, and V2 files were unchanged by that update.

V3 is a separate manuscript. V2's Markdown, LaTeX, PDF, build script, and shared template/filter are preserved unchanged. The new Markdown is the editable source; the corresponding LaTeX and PDF are generated from it.

The abstract and introduction now explain the goal of physiologically meaningful representations, connect it to specific gait measurements in primary clinical research, and distinguish that motivation from the present coordinate-derived target. Technical terms are introduced in context, including latent space, readout, token, source, fold, and seed. Repeated arguments and the long related-work catalogue have been shortened.

The underlying numerical results are unchanged. Corrections clarify the difference between augmentation's effect on learned tokens and the readout's analytic sign guarantee, the order of cohort filtering and source splitting, the unchanged feature channels in the reflection test, and the interpretation of intervals crossing the 0.10 threshold. The pilot's missing artifacts and study-specific methods remain explicit.

The Results revision replaces the 11-row, mixed-metric main table with three questions about prediction. Each row uses the same quantity, change in R-squared, and states the supported inference. The third comparison holds the sign rule fixed, so its negative result concerns pretraining under that rule rather than the effect of adding the rule. The reflection-error improvement is reported separately, with its magnitude and uncertainty. This keeps the positive geometric finding visible without treating it as evidence of better prediction.

The subsequent appendix revision removes the 11-row table from A.5 and the repeated findings table from Appendix C. A.5 now explains the key laterality findings in three connected paragraphs, referring to the main prediction table instead of requiring readers to decode labels such as “native” and “odd/zero.” The reflection-error intervals needed to assess the initialization comparisons and the operational threshold are retained beside that test in A.3. Redundant absolute scores are omitted from the paper; the complete numerical reports remain unchanged. Appendix C is removed because its findings and limitations are already covered in the Results, A.5, and the separate pilot appendix. The remaining classification table is now Table 2. The figure caption explains its existing labels directly, and its graphical content is unchanged.

Laterality scores, interval bounds, and target summaries are now rounded to three decimals throughout the manuscript, including the abstract and appendices. This retains small effects and interval endpoints near zero without reproducing five-decimal computational output. All reported intervals retain their relationship to zero and to the operational reflection-error threshold after rounding. The relative error reduction is described as about 7%. Differences and percentages use unrounded estimates, as Section 3.5 explains; the underlying result files are unchanged. Exact counts, hyperparameters, and numerical tolerances retain their specified values. The shared vector figure already uses three-decimal effect labels and needs no changes.

The classification check is explained using the 10/20 and 6/20 correctly classified video counts implied by notebook 06's saved source-level accuracies; the class-balanced scores and missingness comparison remain in Appendix B, rounded to two decimals for this descriptive 20-video evaluation. The Brain–Body classification notebook and the verified laterality experiment provide evidence for different comparisons within the paper's shared question. Their models, preparation rules, and evaluation designs remain distinct, and the overlapping recordings are not counted as independent replication. Temporal and drift diagnostics were reviewed but do not answer the main laterality question, and no forecasting result is available; they were not added to the main results. No experiments were rerun or underlying numerical findings changed.

The Abstract, Results, and Conclusion foreground laterality's contribution: an anatomically defined movement target with a known response to left/right exchange. Results begin with the improvement in feature consistency from mirrored training, then use the compact prediction table to distinguish that improvement from downstream prediction and from the fixed sign rule. Lower video-balanced reflection error with mirrored training was verified separately for each of the five initializations; this describes consistency of direction, not significance for every initialization or improvement for every individual fitted model. The negative prediction comparison holds the sign rule fixed and concerns baseline pretraining versus its matched initial state. No laterality-supervised encoder loss or clinical validity is implied.

The integrated rewrite removes the Study A/B labels and the narrative of earlier or separate studies from the manuscript, including its loss subscripts. The paper now follows one question: what learned pose features add beyond anatomical structure and simple input summaries. Laterality supplies the matched pretraining comparisons, and exploratory classification supplies an input-baseline check for dataset labels. Classification is discussed within the prediction-results section instead of receiving a standalone subsection. The main methods explicitly identify its different encoder, label-ordered training curriculum, single split, and single initialization; Appendix B retains its overlapping cohort, unverified run settings, and absent initialization control. The discussion integrates the lessons without implying a common cause, a shared encoder, or a classification benefit from reflection. Section 6 is shorter while retaining the substantive cohort, training, and evaluation safeguards.

Appendix A.2 now explains the roles of the VICReg-style terms and the coefficient pattern, with view-averaging conventions made explicit. The 0.05 multiplier remains a fixed setting; no coefficient sensitivity study or gait-specific optimality is claimed. The original loss values and implementation are unchanged.

The subsequent abstract and conclusion refinement replaces the abstract's effect sizes, intervals, and classification counts with qualitative findings. Only the 625-clip, 93-video cohort description remains numerical. The abstract now moves from motor-function motivation to the laterality test, matched encoder comparisons, the distinction between reflection consistency and prediction, and the role of baselines. The fixed-rule negative comparison remains fully reported in Results and Appendix A; its detailed experimental branch is omitted from the abstract. The conclusion gives more space to future learning objectives that preserve left–right movement relationships and to independent gait validation, without presenting those objectives or physiological relevance as achieved. Methods, results, tables, and other sections are unchanged by this refinement.

The rebuilt abstract and conclusion were checked in the PDF, with five main-text pages retained. The wording specifies that the sign rule works with untrained encoder features, without implying that the regression weights are unfitted. An initial rebuild produced a mild underfull-page warning without visible clipping or overlap; the subsequent cohort-description edit removes that warning. No missing-citation, missing-glyph, or text-overflow warning is present.

The abstract's cohort description now identifies gait clips and states that dataset annotations cover normal gait and several neurological and muscular conditions. This describes annotation coverage without presenting the categories as verified diagnoses or suggesting that they supervise laterality pretraining. The test-video separation and repeated-initialization description is retained in the following sentence. GAVD's name, citation, and full category list remain in Methods. No other manuscript text changes in this edit.

The contribution-focused abstract revision makes the study's controlled laterality evaluation explicit: a movement quantity derived from left–right landmark speeds and matched comparisons before and after encoder pretraining. It restores the qualitative finding that pretraining without mirrored examples gives poorer predictions when the sign rule is held fixed, without restoring numerical effect sizes. The exploratory classification result remains in the main Results and Appendix B but is omitted from the abstract so the stronger laterality evidence receives the emphasis. The implications for larger body models are stated as motivation for future evaluation; the experiments concern a compact pose encoder and do not establish clinically meaningful features or the benefit of explicit laterality supervision. The accepted cohort description and all manuscript text outside the abstract remain unchanged.

The YouTube-provenance clarification identifies the 93 sources as selected YouTube videos from an annotated gait dataset, used to study normal gait and neurological and muscular conditions. Every retained clip was matched to its original annotation URL: all 625 clips map to 93 unique YouTube sources. Methods distinguishes GAVD's original manual sourcing and clinician screening from this study's annotation-category selection and pose-quality/target-computability filtering. The final 93-video count is an eligible-cohort count, not a claim that the authors independently searched YouTube and manually selected exactly 93 uploads. GAVD's original data-collection description was checked in Section 3.2.1 of the [dataset paper](https://arxiv.org/pdf/2407.04190), and its [repository](https://github.com/Rahmyyy/GAVD) confirms the YouTube origin. No results, cohort membership, review statuses, or numerical estimates change.

The dataset-credit revision names the Gait Abnormality in Video Dataset (GAVD) in both the abstract and its first body mention, using the dataset's official name. The abstract describes it as a manually curated resource for clinical gait analysis and identifies the 625-clip, 93-video cohort as our quality-screened subset. Methods credits the original creators' sourcing and clinicians' screening and annotation, then describes our selection and eligibility checks. The strengths paragraph acknowledges that foundation while retaining every quantitative evaluation safeguard. Both appendix cohort introductions now identify their source as GAVD, without changing their distinct counts, filtering order, or overlap disclosure. Introduction, Results, Conclusion, model details, and the responsible-use paragraph need no repeated curation account and remain unchanged. Bibliographic article titles retain their published wording; the shared bibliography and V2 are untouched. No curator time estimate, independently verified diagnosis, or new dataset-creation claim is introduced.

The submission-facing cleanup removes machine-specific run identifiers, notebook numbering, dated processing logs, and file-provenance commentary from the manuscript. Dataset selection and numerical checks are described as scientific procedures that do not require access to a particular computer. The pilot's unverified configuration and the unresolved ethics and data-use reviews remain explicit. Internal build instructions and provenance records are kept separate from the paper, and the numerical findings are unchanged.

The PDF uses the shared official NeurIPS 2026 workshop style through a V3-only template. It now contains five main-text pages, four appendix pages, and one reference page. The Results section occupies one page. The existing vector figure is displayed at its full 5.5-inch width. All pages were visually checked during V3 preparation; revised pages were checked again after the precision and appendix revisions. The original detailed table's 33 numerical entries were previously compared with the full-precision reports. After its removal, the retained intervals in A.3 were checked against those reports, and the main prediction and pilot table values were confirmed unchanged. The build has no missing citations, missing glyphs, or overflowing text. The three recorded project reviews remain unresolved; this is an internal draft, not submission approval.

The conclusion now explains the reflection-consistency result and the built-in prediction rule in plain language. It states that the consistency improvement is relative to training without mirrored examples, leaves the prediction benefit unresolved, and explains why a rule that also works with an untrained encoder cannot establish useful pretraining.

Section 6 is now titled “Strengths, limitations, and responsible use.” It leads with the verified cohort construction, source-held-out fitting and tuning, all 50 trained models, matched starting weights and training budgets, and source-balanced scoring with video-level resampling. References to GAVD and grouped cross-validation support the dataset context and evaluation design. The earlier blanket description of small, overlapping cohorts has been replaced with specific limits: unseen-person and external-cohort performance, clinical validation of the motion measure, and the separate pilot's unverified training settings. Training repetition is not presented as adding independent participants or datasets, and the unresolved reviews remain explicit.

The responsible-use paragraph explicitly acknowledges that GAVD's annotations are distributed under the MIT License, which permits reuse beyond academic research subject to its notice requirements. A direct public-license reference has been added. The separately hosted source videos and study-specific ethics, video-use, and derived-pose reviews are distinguished from repository permission; no review status was changed.

From the repository root, rebuild only V3 with:

```sh
bash neurips-brain-body/docs/build_submission_v3.sh
```

The build now regenerates the V3-specific figure from the verified aggregate reports before producing LaTeX and PDF. The additional primary clinical reference is in `bbfm2026_paper_V3_references.bib`; the shared bibliography is unchanged.

The Figure 1 redesign now separates “Effect of encoder training” from “Effect of adding mirrored training clips,” with an explicit reference beneath each heading. The first compares a trained encoder with the same encoder at its random starting weights; the second compares two trained encoders, with training without mirrored clips as the reference. Panel A states “Training excludes mirrored clips” once above its first two rows, then labels their differing prediction rules “Sign reversal not enforced” and “Sign reversal enforced for both encoders.” Panel B labels the training conditions and states that features are compared directly without a prediction rule. The caption defines the sign guarantee and explains the separately fitted regressions. The V3-only generator reuses the original generator's verified data-loading and contrast-selection function without calling its rendering or export functions; earlier figure assets and manuscripts remain separate. All six original full-precision estimates and intervals are retained as source evidence. Panel B displays their sign-reversed reductions, with displayed estimates rounded to three decimals.

The redesigned figure uses two stacked panels at a fixed 5.5 × 6.3 inches, with text of at least 9.7 pt. Neutral circles identify pretraining comparisons and teal diamonds identify comparisons with and without mirrored training clips. Each axis has a visible zero reference, a positive tick, and a positive-is-favorable direction. The six source contrast records and their full-precision intervals match the earlier figure; Panel B's display conversion is recorded separately. Automated checks cover text collisions, publication bounds, label/plot separation, editable SVG text, and absence of raster images. PDF fonts are embedded. The rebuilt paper retains five main-text pages and ten pages overall; the enlarged figure and caption are together on page 8, the complete Results section remains on page 4, and the shortened A.5 explanation fits on page 7. Final page renders were inspected, and the build has no missing citations, missing glyphs, or overfull/underfull boxes. The pre-existing style-package source-encoding warning remains, with no visible missing character. V2 and the shared figure assets were verified unchanged.

The diction revision names the encoder states and training conditions directly instead of using temporal subtraction shorthand. The figure's references now appear explicitly below each group heading, without requiring readers to infer a first-minus-second convention. The caption and Methods 3.4 clarify that prediction regressions are fitted separately while encoder weights stay fixed; “untrained” therefore describes only the encoder. The axes retain “Difference in R² / Positive = better prediction” and “Reduction in disagreement / Positive = closer agreement.” The caption defines both differences and states that an interval crossing zero includes possible gains and losses. “Ordinary prediction” has been replaced with an explicit description of prediction without enforced sign reversal in Methods, Results, the table caption, and A.5. The abstract, Conclusion, table rows, displayed equations, and Appendix B are unchanged by this label revision. Three subagents separately handled the figure layout, editorial review, and numerical/implementation verification. No experiments were rerun.

The Panel B clarity revision explains the comparison between the encoder's features for an original clip and its mirrored copy, with a subtitle in the figure and an ankle-matching example in Section 3.5. The disagreement score q remains nonnegative and unitless; the displayed reduction is reference disagreement minus comparison disagreement. Panel A keeps its original prediction-score differences. Panel B's displayed triples are obtained by negating each original estimate and negating/reordering its interval bounds, giving −0.031 [−0.048, −0.016], −0.022 [−0.039, −0.008], and +0.008 [0.007, 0.010]. Source rows and displayed rows are recorded separately, with checks for exact round-trip recovery, source immutability, interval ordering and width, and preservation of zero inclusion. A.3 retains the original error-increase statements and explains their negative position on the reduction axis; A.5 explains both the positive mirroring comparison and the negative pretraining comparisons. The caveat that nearly input-insensitive features can agree closely remains in Methods. The abstract, Conclusion, all table rows, and Appendix B were unchanged by this revision. An independent final review found the code, numerical records, caption, and appendix explanations consistent.

The Appendix B clarity revision replaces “curriculum” with a direct account of training in stages: normal-annotated clips first, then cumulative introduction of the Parkinson's, stroke, myopathic, and cerebral-palsy categories. Earlier categories remain available, and the order's benefit is not claimed as an experimental finding. The text distinguishes labels used to schedule encoder training from labels predicted by the subsequently fitted classifier. It retains the final 639-clip, 97-video cohort, the 59/18/20 video split and associated clip counts, assignments fixed before pose-quality checks, and overlap with the laterality cohort.

Table 2 now reports videos correctly classified and balanced accuracy, with the latter defined as equally averaged within-category accuracy. Feature-width arithmetic and Macro-F1 were removed from this summary; the three methods retain the same ranking on both omitted and retained scores. Independent recalculation from the notebook's stored predictions confirms 10/20, 6/20, and 6/20 correct videos, with balanced accuracies rounded to 0.44, 0.26, and 0.25. All three methods misclassify the three stroke-annotated test videos. “Missingness control” is explained as the landmark-visibility-only input, without implying that the learned classifier has been shown to rely on missingness.

The appendix omits the loss equation, unverified default hyperparameters, disabled-loss coefficient, and low-level architecture and masking details. Their removal does not turn defaults into verified settings: uncertainty about the exact encoder-training settings remains explicit. The retained qualifications include one split and initialization, no matched untrained-encoder comparison, low-confidence landmarks entering encoder features, and the difference between fitting on video-averaged features and testing through averaged clip probabilities. Classifier fitting, validation-based selection, fixed encoder weights, refitting on 77 development videos, and exclusion of test videos remain described. The relevant main-text cross-references use the same terminology. No experiments were rerun, and no numerical evidence was changed.

The V3-only table layout was adjusted for three columns. The rebuilt paper keeps five main-text pages and ten pages overall, with Appendix B and its table together on page 9. Its layout and retained scientific statements were independently reviewed. Figure 1 and its source evidence remain unchanged, and V2 was preserved.

The joint Appendix A/B revision organizes the laterality account around preparing clips, training with and without mirrored examples, testing prediction and feature agreement, checking uncertainty, and interpreting the findings. It removes redundant cohort inventories, feature-width arithmetic, optimizer-detail lists, and the separate disagreement pass/fail threshold catalogue. The unfavorable pretraining comparisons remain visible in Figure 1 and the text; removing that catalogue does not support an absolute claim of reflection consistency. The loss equation and the explanation of the 0.05 multiplier and VICReg's 25/25/1 coefficients remain, with no claim that their suitability for gait has been established.

The revised preparation description distinguishes the original observations used to calculate laterality from interpolated encoder inputs. It retains matched left/right observations, video separation, coverage requirements, and the requirement for at least two complete target-landmark patches. The last requirement was verified in the existing preparation code and is newly described, not newly imposed. The prediction account retains separately fitted regressions, the conditions needed to guarantee sign reversal, training-only tuning, and the qualification that inner validation inputs may already have been used for encoder training. The feature-disagreement equation is unchanged and now has a direct explanation of landmark matching, its denominator, and its limits. Equal video weighting, paired video resampling, fixed-model uncertainty, and the absence of external preregistration remain explicit.

Appendix B now gives the overall clip count and the independent video counts without the redundant per-split clip inventory. Its third input is named "landmark availability only," reflecting the usable/missing observations from which it is calculated. The text explains staged category introduction, classifier fitting and selection, and the differing ways clips are combined during fitting and testing. Overlapping recordings, the single split and initialization, the absent matched starting-weight comparison, and unverified encoder-training settings remain disclosed. All retained table values were checked against the saved predictions.

Numerical precision follows the size and interpretation of each quantity: laterality effects and interval bounds retain three decimal places so that a change of +0.004 and an interval endpoint of +0.002 do not disappear through rounding; classification balanced accuracies retain two decimal places alongside exact counts of correctly classified videos. The relative feature-disagreement improvement remains "about 7%." These display choices do not imply clinical measurement precision. Retained experimental settings retain their actual values, and omitted implementation tolerances have not been rounded into different settings.

The Markdown, LaTeX, and PDF were synchronized and the retained evidence independently reviewed. The main-text cross-references were aligned with the new appendix headings and terminology. The abstract, conclusion, main table rows, displayed equations, Figure 1 caption, and figure assets are unchanged by this revision; V2 is also unchanged. The final PDF retains five main-text pages and ten pages overall. Results remain together on page 4, Appendix A occupies pages 6–7, Figure 1 and its full caption remain on page 8, and Appendix B fits on page 9. A V3-only layout rule keeps the disagreement equation with its explanation. Visual inspection found no clipping or stranded headings, and the build reports no missing citations, missing glyphs, or overfull/underfull boxes. No models were retrained and no underlying results were changed.

## Pipeline overview and Figure 2 redesign

Appendix C adds one method overview connecting video-based pose extraction, encoder pretraining, and the controlled laterality evaluation. It addresses a useful explanation gap without adding a collection of notebook charts. The available charts either repeat reported findings or concern exploratory settings without the matched controls needed for the main pretraining claims. No new numerical finding is introduced.

The new account was checked against the extraction notebook, preparation code, and completed laterality configuration. It distinguishes estimated pose from calibrated motion capture, separates the original observations used for laterality from prepared encoder inputs, and identifies the running-average target encoder as the encoder evaluated after training. Its saved random starting weights provide the matched reference. The pretraining predictor and laterality regressions have separate roles; additional feature regularization uses separate unmasked views. Appendix C cites S-JEPA as the source of the adapted approach rather than implying an exact reproduction of its published setup.

Following the request for a cleaner Figure 2, the initial paragraph-heavy schematic was replaced with three numbered visual stages: Prepare pose, Train encoder, and Freeze and test. Simple vector glyphs and short labels reduce the displayed text from 158 to 46 words, about 71% fewer. The figure is 5.5 × 4.8 inches with text of at least 10.2 pt. The gold laterality path bypasses encoder pretraining, weight-transfer arrows originate on encoder glyphs, and locks distinguish the frozen evaluation states. The two prediction glyphs represent separately fitted regressions. Both original/mirrored feature-agreement glyphs use the same neutral color to avoid suggesting a comparison between different encoders. All glyphs are schematic; no participant images, individual pose traces, or invented results are shown.

The caption explains training-video fitting, held-out testing, and left/right landmark matching, while Appendix C carries the implementation detail. The generator validates the method configuration and checks text bounds, overlaps, connector placement, editable SVG text, and absence of raster images. The PDF has embedded searchable fonts. Independent semantic review and a full-page render check found the simplified figure consistent with the methods and caption.

Markdown, LaTeX, and PDF are synchronized. Appendix C, Figure 2, and its caption fit together on page 10; references occupy page 11, with five main-text pages retained. There are no missing citations, missing glyphs, or overfull/underfull boxes. The pre-existing style-package encoding warning remains without a visible missing character. The existing manuscript text is unchanged except for the appendix cross-reference, and Figure 1 and the V2 manuscript files were verified unchanged. No models were retrained or inference rerun. The separate workspace title edit, **What Do Self-Supervised Pose Representations Add Beyond Anatomy? Lessons for Body Foundation Models from Gait Laterality**, was preserved and propagated to the generated files. Existing responsible-use review holds remain unchanged.
