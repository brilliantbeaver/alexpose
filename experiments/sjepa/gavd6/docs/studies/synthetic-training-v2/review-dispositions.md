# Review findings and dispositions

Separate Codex contexts inspected the actual protocol, implementation and saved artifacts. Available worker model: `gpt-6-astra`, high reasoning. No `ultracode` interface was available. Reviews were bounded and began read-only; later implementation ownership was disjoint. A worker's review of their own subsequent correction is explicitly distinguished from independent verification. These reviews validate software/evidence boundaries within their scope, not the study's scientific hypothesis.

| Review | Supported finding | Disposition and verification |
|---|---|---|
| [Protocol R1/R2](protocol-review.md) | Gate rules and preservation measures were underspecified | Added exact coordinate/displacement, signed separation, RMS amplitude, operational maxima, support/degeneracy rules. Source adjudication requires retained independent calibration and at least three seeds. No calibration is fabricated; confirmation remains pending. |
| Protocol R3 | Naturally missing queries and teacher geometry could differ between objectives | Matched input-only artificial masks plus missing-input restoration queries; clean-valid token support and reduction match coordinate/paired JEPA. Both targets use noisy-input normalization. Independent missing-target/isolation and query-hash tests pass. |
| Protocol R4 | Practical refiner adaptation/loss was underspecified | Documented temporal MLP and supported reference-acceleration L1 loss; initial weight 0.1, declared development grid 0/0.1. Attenuation/missing-triple test passes. This is a source-trained adaptation, not an author-model reproduction. |
| Protocol R5 | Exclusion assignments and unusual-motion references were not frozen | Source preparation now requires authoritative reservation/exposure manifests and family exclusion. Actual cohorts/extractors, unusual-motion references and calibration remain empirical prerequisites. |
| [Implementation R1–R4](implementation-review.md) | Missing-score adapter mismatch; alias identity loophole; dropped reservations; NaN/negative budget loopholes | Numeric fill preserves observed flags and raw missing scores. Known original identities cannot change canonical mapping. Reservations are excluded before motion reads. Budgets require finite nonnegative values and a matching actual prior ledger. Five reviewer-authored adversarial tests pass. |
| Implementation R5/R12 | Held families and source-producing bytes were not fully bound | Preparation excludes the entire held family from training. Identity binds producer code, inherited configs, actual weights, registry/audit/reservations, full rendering/body assets and motion contents; shape uses actual betas and depth is recorded. Six provenance tests independently run by coordinator pass. The statistics reviewer implemented these corrections; they are not labeled independent endorsement by that same worker. |
| Implementation R8/R9/R11 | Mutable runtime record, incomplete cumulative spending, no workflow resume | Runtime records are immutable per attempt; all allocated attempts and failures count, with a shared scope budget lock plus scheduler bounds. Explicit `--resume` uses trainer identity/RNG/optimizer validation. Budget serialization/failure accounting and exact trainer resume tests pass. No GPU operation was executed to test these branches. |
| [Results receipt findings](results-review.md) | Foreign/stale receipts, transitive tampering and incomplete artifact ownership | Validate run/stage identity and dependency hashes; completed-stage outputs include existing partial artifacts not owned by another receipt. Five independent workflow tests pass. Final reviewer checked 10 receipts and 117 owned artifact hashes. |
| Results source-gate finding | A nonempty calibration hash string could masquerade as evidence | Added actual file/SHA256 verification, compatible schema/time/evidence metadata, reviewer/reference provenance, candidate-output exclusion and exact margin binding. Two adversarial calibration tests pass. Source benefit remains untested. |
| [Readability review](readability-review.md) | Ambiguous method names, synthetic groups, motion events and bootstrap interpretation | Added method key, analytic-ID qualification, exact units/support counts, paired-JEPA candidate/sign, nested resampling and excluded training-selection uncertainty. Revised notebook captions avoid presumed improvement or novelty. |
| Readability/results plot finding | Overlapping labels hid method comparisons | Regenerated a new immutable run with separate extractor panels and external color/shape legend; independently viewed and verified. Coincident points require the numeric table for exact differences. |
| Readability next-step suggestion | Report did not order practical next actions | Ordered next steps are in the [delivery report](report.md) and [HAIC runbook](../../../slurm/synthetic-training-v2/README.md). The prior report artifacts remain unchanged. |

The final reviewed run is [`fixture-notebooks-20260918-final`](../../../outputs/synthetic-training-v2/fixture-notebooks-20260918-final/report.md), computed from scratch through nine fresh kernels. The earlier partial integration attempt and intermediate validation runs remain available under their original unique identities; none is reclassified as source or confirmation evidence.

Independent final reconstruction matched 432 per-window rows and 24 primary summaries; five metrics agreed within 1e-16. The reviewer independently reconstructed 100 nested-bootstrap draws and their interval, checked all artifact hashes, and found 19 executed code cells across nine notebooks with zero errors. The reviewed snapshot keeps `confirmation_opened=false`.

The strongest competing explanation remains cleaner supervision, ordinary denoising or readout capacity rather than latent prediction. No software test, low JEPA loss, effective rank or smoother fixture output settles that explanation. There is no positive empirical scientific gate, real-transfer evidence, clinical claim or confirmed novelty result in this delivery. Gate A, Gate B, real transfer and video are insufficient evidence; personalization is closed on the old development panel only.
