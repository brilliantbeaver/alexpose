TASK: Repair and run the three JEPA architecture variants as a scientifically valid, rapid experiment

Workspace:
/Users/theodoremui/dev/alexpose/experiments/sjepa/gavd6

Primary package:
- src/gavd6_sjepa/gait.py
- src/gavd6_sjepa/amass.py
- src/gavd6_sjepa/train_amass.py
- tests/test_amass_core11_jepa.py

Goal
====
Implement the minimum model, training, and evaluation changes required to compare three JEPA architectures scientifically:

1. paired_shared_no_cross: shared branchwise encoder and predictor, no cross-attention.
2. reflection_equivariant: symmetric, swap-commuting cross-attention.
3. paired_unconstrained: direction-specific, untied cross-attention.

The experiment should answer:

"At matched model capacity and training exposure, does symmetric cross-branch fusion outperform no fusion and unconstrained fusion?"

Prioritize rapid experimentation and trustworthy results. Do not spend time on provenance hashes, elaborate manifests, resumable jobs, atomic artifact machinery, or production hardening. Preserve unrelated existing changes in the dirty worktree.

Background and confirmed problems
=================================
The existing reflection-equivariant encoder is geometrically correct: it commutes exactly with reflection. Its poor final checkpoint was primarily a learning-architecture problem, not a failed mirror operator.

The current paired encoders and EMA teachers use cross-branch attention, but TokenPredictor restores masked tokens and predicts each branch independently. The teacher therefore constructs cross-dependent targets using complete branches while the predictor lacks cross-branch interaction after mask-token insertion. The reflection run beat the current standard through roughly epoch 36, reached its best total loss near epoch 31, and then regressed badly. A small three-seed diagnostic reduced this late regression when teacher cross-attention was weakened or disabled.

Additional problems:

- Cross-attention residuals are added at full strength with no gate or LayerScale.
- The model called "standard" is actually a paired, shared, no-cross encoder. Rename it honestly; do not silently treat it as a single-view JEPA.
- paired_unconstrained has nearly twice as many encoder parameters as reflection_equivariant at the same width, invalidating a clean weight-tying comparison.
- Only final checkpoints are currently saved. The trainer does not perform validation-based checkpoint selection.
- Training histories omit teacher entropy, KL divergence, full VICReg components, and representation-health metrics.
- The current run used only seed 7. Seeds 19 and 31 were planned but not completed.

Required architecture changes
=============================

A. Shared no-cross baseline
---------------------------
- Rename the current "standard" variant to "paired_shared_no_cross" throughout active code, CLI choices, outputs, and tests.
- Keep one shared branchwise encoder applied independently to original and mirrored branches.
- Keep a shared branchwise predictor with no cross-attention.
- Keep paired inputs, masks, JEPA loss, and parity-resolved VICReg so its training objective remains comparable to the paired models.
- Do not add a true single-view model in this task. That would be a fourth arm with a different objective.

B. Reflection-equivariant model
-------------------------------
- Retain the symmetric encoder construction F(a,b) = [g(a,b), g(b,a)].
- Add a symmetric paired predictor. It must:
  1. Restore mask tokens and positional embeddings in both branches.
  2. Process the complete reconstructed branch pair with symmetric self/cross layers.
  3. Return predictions at masked positions from both branches.
- The predictor must commute with branch swapping exactly, including after mask-token insertion.
- Keep the target encoder as an EMA copy of the online encoder. Do not permanently remove cross-attention from only the target encoder.
- Gate every cross-attention residual using a shared scalar or LayerScale that preserves swap equivariance. Initialize its effective scale near 0.1. Apply the same principle in the paired predictor.

C. Paired-unconstrained model
-----------------------------
- Add an unconstrained paired predictor with separate self-attention and directional cross-attention modules for A->B and B->A.
- Restore mask tokens before paired predictor interaction, matching the information flow of reflection_equivariant.
- Use the same initial cross-residual scale as reflection_equivariant.
- It is expected to violate branch-swap commutation. This is intentional.

D. Capacity matching
--------------------
- After implementing the predictors, count all trainable parameters that affect optimization: online encoder, predictor, cross gates, and VICReg projector(s). Exclude the frozen EMA copy from the trainable count.
- Adjust embedding width and/or feed-forward width per variant until total trainable parameter counts are within approximately 5% of one common budget.
- Keep depth, mask fraction, window exposure, and number of optimizer updates matched unless a documented compute-matched secondary run is added.
- If exact matching is not practical, stop and report the remaining difference before launching expensive full runs. Do not claim that weight tying caused an effect when capacity is materially different.

Loss and metrics
================
Keep the same primary training objective for all three variants:

total_loss = jepa_loss + vicreg_weight * vicreg_loss

For the first repaired experiment, retain the current temperatures, mask fraction, yaw augmentation, VICReg weight, odd-channel weight, optimizer family, and weight decay. Do not simultaneously redesign VICReg.

Extend loss reporting to include:

- JEPA cross-entropy.
- Teacher entropy H(q).
- KL divergence D_KL(q || p) = JEPA cross-entropy - teacher entropy.
- Even VICReg invariance, variance, and covariance.
- Odd VICReg invariance, variance, and covariance.
- Total VICReg loss and total loss.

KL must be computed from the same teacher and predictor distributions used in JEPA cross-entropy. Validate numerically that CE = entropy + KL within floating-point tolerance.

Training-loop changes
=====================
Refactor the monolithic fixed-update trainer into an epoch-oriented workflow:

for each epoch:
  1. Train for one complete epoch of matched window exposures.
  2. Evaluate the validation split with model.eval() and no gradients.
  3. Record training and validation metrics.
  4. Save a latest checkpoint.
  5. Update a best checkpoint using the rule below.

Use a maximum of 100 epochs and early stopping with approximately 10-15 validation epochs of patience.

Checkpoint selection:

1. Reject checkpoints with non-finite metrics or obvious representation collapse.
2. For reflection_equivariant, also require the complete commutation audit to pass.
3. Among eligible checkpoints, select the lowest validation KL. If teacher entropies are effectively equal across variants/checkpoints, validation JEPA cross-entropy may be reported alongside it.

Do not select checkpoints using training loss. Do not automatically use the final epoch.

Validation and test evaluation
==============================
Create a shared evaluate_variant(...) path used by every architecture.

Requirements:

- Use the existing identity-disjoint train, validation, and test splits.
- Use fixed validation masks and augmentation draws so every variant sees the same evaluation task.
- Use several deterministic mask/view draws per validation window if inexpensive, then average them.
- Never update model, projector, center, or EMA state during validation/test.
- Evaluate the test split exactly once after architecture and hyperparameters are frozen, using the selected best checkpoint.

Return at least:

- JEPA cross-entropy, teacher entropy, and KL.
- All even/odd VICReg components.
- Even/odd feature energy and feature variance.
- Effective rank and mean pairwise cosine for pooled even/odd representations.
- Layerwise and final commutation residuals for paired_shared_no_cross and reflection_equivariant.
- The expected nonzero commutation residual for paired_unconstrained.

Health metrics prevent a collapsed or trivial teacher from winning merely because its KL is small. Do not interpret low JEPA/KL alone as proof of a useful representation.

If a downstream target/probe already exists and can be run cheaply, freeze each selected encoder and evaluate every variant with the identical probe, split, regularization-selection procedure, and metric. Without a downstream evaluation, limit conclusions to masked prediction, geometry, and representation health.

Run plan
========
Stage 1: implementation checks
- Add focused unit tests for predictor output shapes, mask restoration, cross-gate behavior, and commutation.
- Confirm paired_shared_no_cross and reflection_equivariant commute in online encoder, EMA encoder, predictor, and masked predictions.
- Confirm paired_unconstrained generally does not commute.
- Run all existing relevant tests.

Stage 2: smoke experiment
- Run every variant for 2-5 epochs on the small/smoke configuration.
- Verify finite losses, CE decomposition, non-collapsed even/odd features, working validation, checkpoint selection, and reloadable best checkpoints.

Stage 3: full pilot
- Run seed 7 for all three variants on the full AMASS Core11 training split.
- Use the same window order, masks, augmentations, and exposure budget within the seed.
- Inspect validation curves before launching more seeds.
- If reflection still shows late regression, preserve the best validation checkpoint and report the trajectory rather than tuning on test results.

Stage 4: scientific comparison
- Once the architecture and selection rule are frozen, run paired seeds 7, 19, and 31 for all variants.
- Compare variants within matching seeds.
- Report per-seed results plus mean and standard deviation.
- Test only the selected checkpoint from each completed run.

Rapid tuning budget
===================
If tuning is required, keep it small and validation-only:

- Learning rate: {1e-4, 2e-4}.
- Initial cross scale for paired models: {0.1, 0.25}.
- Keep predictor depth fixed initially.
- Give reflection_equivariant and paired_unconstrained the same number of tuning trials.
- Do not inspect test results while choosing settings.

Acceptance criteria
===================
The task is complete when:

1. All three variants have matched student/teacher/predictor information flow appropriate to their architecture.
2. Cross-attention is gated and stable at initialization.
3. Total trainable parameter counts are within about 5%, or the mismatch is explicitly surfaced before full training.
4. Reflection commutation holds through encoder, EMA teacher, predictor, and masked outputs.
5. Training and validation log CE, entropy, KL, complete VICReg components, and representation-health metrics.
6. Best checkpoints are chosen exclusively from validation results and survive reload/evaluation.
7. Test evaluation uses only the selected best checkpoints and does not mutate model state.
8. A full seed-7 pilot completes for every variant under a matched exposure schedule.
9. If resources permit, seeds 19 and 31 complete under the frozen protocol.
10. The final report clearly distinguishes optimization/geometry evidence from downstream representation utility.

Deliverables
============
- Minimal source changes implementing the corrected architectures and evaluation loop.
- Focused tests for the new architecture contracts and validation behavior.
- A concise command or script for smoke, full seed-7, and three-seed runs.
- Training/validation histories for each run.
- Best checkpoints and one final summary table with parameters, runtime, selected epoch, validation metrics, test metrics, representation-health metrics, and commutation results.
- A short explanation of any remaining scientific limitations.

Working rules
=============
- Inspect the current dirty worktree before editing; preserve unrelated user changes.
- Prefer the smallest coherent implementation over framework or artifact refactors.
- Do not launch expensive full runs until architecture tests, metric decomposition, validation selection, and a smoke run pass.
- Do not use the test split for tuning, checkpoint selection, or early stopping.
- Do not describe the current no-cross baseline as single-view.
- Do not claim an equivariance advantage unless capacity and training exposure are adequately matched.
