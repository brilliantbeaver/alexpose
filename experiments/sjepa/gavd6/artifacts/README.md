# Local artifacts

This directory contains small, repository-level research records. Large or
machine-specific experimental outputs belong under the configured `work/` or
AMASS run root.

- `research/`: compact research bundles retained with their related methods.
- `notebook_runs/`: optional executed notebook copies; ignored by Git.

Source notebooks always live under [`notebooks/`](../notebooks/README.md).
