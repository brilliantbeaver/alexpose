# Historical manual-install constraints, retained for the repair reports.
# Routine setup now uses pyproject.toml + uv.lock via setup-environment.sh.
# Compatible Python 3.11 MMPose stack; install Torch from the CUDA 12.1 index.
numpy==1.26.4
torch==2.1.0
torchvision==0.16.0
mmcv==2.1.0
mmengine==0.10.7
mmdet==3.3.0
mmpretrain==1.2.0
opencv-python==4.9.0.80
# Install from the exact Git revision in README.md; the PyPI release has a different API.
human-body-prior==2.2.2.0
# Official Git revision in README.md fixes Python 3.11 / NumPy 1.26 imports.
chumpy==0.71
