create a meaningful feature branch and commit with detailed well structured notes.  Create PR and merge with origin main.
  Locally checkout main and fast forward.  Delete merged branches both locally and in origin main.
