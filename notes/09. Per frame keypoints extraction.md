# 3. Compute joint angles – For each frame, calculate hip, knee and ankle angles
joint_angles_array = get_joint_angles(keypoints_array=keypoints_array)

systematically and throroughly implement this `get_joint_angles()` function in order to compute an array of joint angles (per frame).  For each frame, calculate hip, knee and ankle angles using the vector dot‑product formula: the hip angle uses points at the shoulder, hip and knee; the knee angle uses hip, knee and ankle; the ankle angle uses knee, ankle and foot. MediaPipe coordinates can be converted from normalized positions to pixels; joint angles measured this way agree well with marker‑based systems (mean absolute error < 5 ° for hip/knee/ankle angles).

Before you start implementing, make sure to thoroughly underestand the current implementation in the `ambient` package: #ambient .  Your work must try to incorporate what has already been implemented -- and continue to use consistent interfaces, protocols, and other data structures.  Refactor `ambient` subpackages to unify the code and abstractions systematically and thoroughly.

Use best OOP software engineering practices.  Create sensible data structure to represent joint angles and frame-based set of joint angles.  Create these data structures in the package `ambient/pose`.  Use best software engineering design principles of SOLID, DRY, YAGNI, modularity, robustness, and extensibilities.  Document your design and architecture clearly in the `docs/gait` folder with tutorial style writings and clear examples.  Fully test your implementation with pytests, property-based tests, and integration tests thoroughly.  Think deeper on your design and architecture before implementation.