For the `frontend` webpp, add a new page for "Realtime" gait analysis.  Add a "Realtime" top nav item to get to that page.  

This Realtime gait analysis page will show a live camera view of the local webcam.  We want to be able to apply MediaPipe pose estimation directly on this live realtime feed and to be able to toggle on/off keypoints as well as skeletal overlay on this live webcam stream. 

Think deeply on how you will enable this real-time pose estimation task. 

Use best OOP software engineering practices, SOLID, DRY, YAGNI, modularity, extensibility and robustness in your design and implmementation.  Test everything with pytest, property-based tests, and integration tests thoroughly.  Fix all remaining issues.  Document your new classifier in the appropriate markdown files in the `docs` folder -- also add that for knn_classifier documentation.

For pose estimation, you must leverage existing implementation in the package: `ambient/pose`  rather than duplicating code.  The existing implementation also enables generic compatibility of different pose estimators.  Carefully and systematically refactor your code carefully.

Similar for "gait_analyzer.py": you must not create duplicate code and functionalities but instead: strongly prefer to reuse and build on the package in `ambient/analysis` as well as the rest of the `ambient` package.

Systematically proceed with your planning, design, implementation and testing.  Think deeper.