Create a KNN classifier in this file "experiments/exp2/src/process4_KNN.py", based on the training data uploaded from "condition_paths" whose "condition_name"'s are the labels for each of the CSV files uploaded and processed.  Use best engineering practices and scikit-learn package.  You must systematically examine and deeply understand "temporal_analyzer.py", "symmetry_analyzer.py", "gait_analyzer.py", and other implementations in the package `ambient` and leverage and build on what has already been scaffolded or started.  You should also illustrate how to use this classifier clearly using tutorial style documentation and code in the Jupyter notebook "notebooks/tutorial2 - train classifier.ipynb".

The program right now has averaged over gait cycle – simply average each angle across the entire walking clip.  Compare the mean left and right joint angles to check for "normal" condition or not.  If not, larger asymmetry suggests stroke.  Reduced hip/knee range‑of‑motion may indicate parkinsons or antalgic.

When training a basic classifier, use the small number of labelled samples from each class (healthy, stroke, parkinsons, antalgic, etc).  Build a simple dataset where each sample contains mean hip, knee and ankle angles plus left–right differences.  Build a k‑nearest‑neighbour (KNN) example with clear documentations (online as well as offline in `docs/classifier` folder).

Before you start implementing, make sure to thoroughly underestand the current implementation in the `ambient` package: #ambient .  Your work must try to incorporate what has already been implemented -- and continue to use consistent interfaces, protocols, and other data structures.  Refactor `ambient` subpackages to unify the code and abstractions systematically and thoroughly.

Use best software engineering design principles of SOLID, DRY, YAGNI, modularity, robustness, and extensibilities.  Document your design and architecture clearly in the `docs/classifier` folder with tutorial style writings and clear examples.  Fully test your implementation with pytests, property-based tests, and integration tests thoroughly.  Think deeper about your design and architecture before implementation.